#!/bin/bash -e

# If running the rails server then create or migrate existing database
# Works for both direct `./bin/rails server` and `./bin/thrust ./bin/rails server`
if [[ "$*" == *"./bin/rails"* && "$*" == *"server"* ]]; then
  ./bin/rails db:prepare
fi

exec "${@}"
